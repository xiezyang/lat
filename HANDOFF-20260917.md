# LAT no-LBT/强制关闭 LBT 当前调试交接

日期：2026-09-17

## 1. 当前结论

- 当前源码被测的是“去除诊断补丁”的发布树，但强制关闭了硬件 LBT。
- 当前测试没有设置 `LATX_SOFTFPU=2`，实际是 no-LBT + `option_softfpu=0`。
- 当前版本不是 debug 构建，运行时没有逐指令、逐 TB 调试日志。
- 当前版本使用 `-O 2`、静态编译、未启用 KZT。
- ELF 文件没有被 strip，仍包含 `.debug_info` 和 `.symtab`，但这不等于 debug 构建。
- 强制 `option_enable_lbt=0` 会关闭多项优化并进入软件 EFLAGS/TOP 路径，导致约 10.5 倍性能下降。
- 已完成并通过 9 个 CINT2000 REF 子项；全量测试在 `255.vortex` 运行期间手动暂停。

## 2. 测试机

- 登录方式：`ssh xzyla`，再 `ssh loongson@192.168.8.7`
- 主机名：`loongson-pc`
- 内核：`Linux 4.19.190-comp+ #2 SMP`
- CPU：Loongson-3A5000-LL，4 核
- CPU 能力：
  `cpucfg lam ual fpu lsx lasx crc32 complex crypto lvz lbt_x86 lbt_arm lbt_mips`
- CPU 实际支持 `lbt_x86`，但当前版本在软件中强制关闭。

## 3. 源码与构建

板卡源码目录：

```text
/home/loongson/xzy/lat-no-lbt-release-20260917
```

构建机：

```text
/home/yuerengan/lala/xzy/lat-no-lbt-release-20260917
```

构建机登录：

```text
ssh xzyla
ssh -p 22522 yuerengan@192.168.8.2
```

构建命令：

```bash
cd /home/yuerengan/lala/xzy/lat-no-lbt-release-20260917
./latxbuild/build64.sh -c -O 2
```

构建配置：

```text
optimization = 2
debug = false
TCG debug = disabled
static build = YES
KZT = 未显式启用
```

测试机上的二进制：

```text
/home/loongson/xzy/lat-no-lbt-release-20260917/latx-release
SHA256 4c7d563203fd1a6fe634243fa9cbd6b88dbdbae8652bbd51f73c5de8374b222e
```

说明：

- `-O 2` 不是 `build64.sh` 默认值；默认是 `-O 1`。
- 选择 `-O 2` 是为了沿用之前已保存的构建方式，不能证明 O2 对当前问题有特殊作用。
- 该源码从 `4e7cdcc1193` bundle 重建，排除了诊断提交 `0013`、`0014`、`0022`、`0023`。
- 保留了 19 个 no-LBT 功能补丁和全局 guest-memory `dbar(0)` 对照行为。
- `RELEASE_SOURCE_INFO.txt` 记录了排除项。

## 4. 强制关闭 LBT 的位置

文件：

```text
target/i386/latx/latx-options.c
```

函数：

```c
void latx_apply_host_hwcap(unsigned long hwcap)
```

当前修改：

```c
option_enable_lbt = 0;
```

因此无论真实 HWCAP 是否包含 `HWCAP_LOONGARCH_LBT_X86`，最终都会执行
`latx_apply_no_lbt_restrictions()`。

该函数会关闭：

```text
LBT
LASX
TU
AOT
JR/RA
tunnel lib
VPAES
fast atomic
inst pattern
AVX optimization
rounding optimization
```

这是当前性能下降的主要原因。

## 5. 运行时模式消息

启动后输出：

```text
LATX: software state mode enabled (LSX=1 LASX=0 LBT_X86=0)
```

含义：

- `LSX=1`：宿主基础 LSX 可用。
- `LASX=0`：该软件状态模式关闭 LASX。
- `LBT_X86=0`：不执行 LoongArch LBT x86 指令。
- 软件 EFLAGS、x87 TOP/tag 等状态改由普通 LoongArch 指令维护。

这不是调试日志。它不是逐指令 trace，也不是 `LATX_TB_STATE`。

## 6. SOFTFPU 情况

当前测试没有设置 `LATX_SOFTFPU=2`：

- 进程环境中没有 `LATX_*` 或 `LAT_*` 变量。
- `/etc/latx-x86_64.conf` 不存在。
- `~/.config/latx-x86_64.conf` 不存在。
- `gcc8-2000-dyn-latx-release.cfg`、`myrun.sh`、`shrc` 均未设置。
- `option_softfpu` 默认值为 0，只有显式 `LATX_SOFTFPU` 或配置才改变。

因此当前测试覆盖的是：

```text
LBT_X86=0 + LATX_SOFTFPU=0
```

没有覆盖：

```text
LBT_X86=0 + LATX_SOFTFPU=2
```

无 LBT 软件状态模式与 SOFTFPU 是两个不同开关。前者处理 x86 EFLAGS/TOP
及部分 LBT 指令语义，后者控制 x87 浮点运算实现。

## 7. SPEC 执行方式

SPEC 目录：

```text
/home/loongson/xzy/spec2000-x86
```

系统 binfmt 仍指向旧解释器：

```text
/usr/bin/latx-x86_64
```

但本次测试没有经过 binfmt。新增配置：

```text
config/gcc8-2000-dyn-latx-release.cfg
```

第 48 行：

```text
monitor_wrapper = /home/loongson/xzy/lat-no-lbt-release-20260917/latx-release $command
```

生成的 `speccmds.cmd` 均以新 LAT 路径开头，例如：

```text
/home/loongson/xzy/lat-no-lbt-release-20260917/latx-release ../../175.vpr/run/.../vpr...
```

`myrun.sh` 已改为允许配置选择，默认行为不变：

```bash
CONFIG=${SPEC_CONFIG:-gcc8-2000-dyn.cfg}
runspec -c "$CONFIG" -i "$1" -n "$2" "$3" -I
```

实际全量命令：

```bash
cd /home/loongson/xzy/spec2000-x86
SPEC_CONFIG=gcc8-2000-dyn-latx-release.cfg \
timeout 14400 \
./myrun.sh ref 1 all
```

这里只额外设置了 `SPEC_CONFIG`。没有设置 LATX 选项、QEMU 选项、
`LD_PRELOAD` 或 `LD_LIBRARY_PATH`。

## 8. REF 已完成结果

以下子项全部通过：

| 子项 | 运行时间（秒） |
|---|---:|
| 164.gzip | 1235.493540 |
| 175.vpr | 896.824977 |
| 176.gcc | 737.278591 |
| 181.mcf | 480.017884 |
| 186.crafty | 686.765371 |
| 197.parser | 1328.871854 |
| 252.eon | 718.771348 |
| 253.perlbmk | 1581.269498 |
| 254.gap | 881.323934 |

当前暂停时：

```text
255.vortex ref 正在运行，尚未完成。
```

未完成项：

- CINT2000：`255.vortex`、`256.bzip2`、`300.twolf`
- CFP2000：`168.wupwise`、`171.swim`、`172.mgrid`、`173.applu`、
  `177.mesa`、`178.galgel`、`179.art`、`183.equake`、`187.facerec`、
  `188.ammp`、`189.lucas`、`191.fma3d`、`200.sixtrack`、`301.apsi`

完整日志：

```text
/home/loongson/xzy/spec2000-x86/ref-all-1-latx-release.log
```

## 9. 性能对照

同机、同 CPU、同输入执行 `164.gzip` 测试输入：

```bash
taskset -c 0 <LAT> gzip_base.Of.gcc830.dyn input.compressed 2
```

结果：

```text
系统 LAT /usr/bin/latx-x86_64：
  899 ms、891 ms、887 ms

新 latx-release，强制 LBT_X86=0：
  9315 ms、9344 ms、9314 ms
```

新版本约慢 10.5 倍。

排除了以下原因：

- 不是使用了 `build64-dbg.sh`。
- 不是 `CONFIG_LATX_DEBUG`。
- 不是逐 TB 调试输出。
- 不是经过旧系统 binfmt。

主要原因：

- 当前测试机支持 `lbt_x86`。
- 新版本强制关闭 LBT，并关闭大量依赖 LBT 或与 LASX/TU/atomic 等相关的优化。
- EFLAGS、x87 TOP 等路径转为软件状态维护。

因此，若正常版本预期 `164.gzip test` 小于 1 秒，强制 `option_enable_lbt=0`
的版本无法满足。正常性能测试应使用启用硬件 LBT 的版本。

## 10. 与旧板卡问题的关系

之前 `root@192.168.8.200` 的故障环境与当前 `192.168.8.7` 不同：

- 旧板卡无 LBT，`175.vpr` 曾失败。
- `context-v2` 原生程序在旧板卡上确认了两个同核进程的 `f16` 互相串用。
- 当前 `192.168.8.7` CPU 明确支持 `lbt_x86`，但测试版本主动关闭 LBT。

不要直接把旧板卡的“宿主 FPU 上下文串用”结论套到当前 `192.168.8.7` 结果。
当前机器上，`175.vpr ref` 已通过。

## 11. 需要另一个 AI 判断的问题

1. 你们要求的目标配置是否是 `LBT_X86=0 + LATX_SOFTFPU=2`？
   当前测试不是这个配置。
2. 强制 `LBT_X86=0` 时关闭 LASX、TU、AOT、atomic 等优化是否是既有设计？
   当前源码就是这样做的。
3. 预期小于 1 秒的性能基线是否允许硬件 LBT？
   若允许，应测试启用 LBT 的版本；强制 LBT=0 版本预计无法达到。
4. 是否需要先完成 `LATX_SOFTFPU=2` 对照，再继续剩余 REF 子项？
5. 是否需要生成 strip 版本？
   当前 `latx-release` 含 DWARF 和符号表，但没有调试输出。

## 12. 已知环境陷阱

- `myrun.sh` 原配置写死为 `gcc8-2000-dyn.cfg`；当前通过 `SPEC_CONFIG` 选择新配置。
- `monitor_wrapper` 中的 `$command` 不能省略，否则原 benchmark 命令会被替换掉。
- 系统 binfmt 仍是旧的 `/usr/bin/latx-x86_64`；判断实际解释器要看
  `speccmds.cmd`，不能只看 binfmt 状态。
- 当前全量运行已暂停，没有遗留的 `runspec`、`specinvoke` 或 `latx-release` 进程。
- 二进制哈希变化后，必须重新确认 SPEC 配置中的 `monitor_wrapper` 指向对应产物。
