name: LAT Tests

on:
  workflow_call:
  # Select the exact release-candidate ref when running manually before release.
  workflow_dispatch:
  schedule:
    # Daily at 02:00 China Standard Time.
    - cron: "0 18 * * *"
  release:
    types: [published]
  push:
    branches:
      - master
    paths:
      - "**/*.c"
      - "**/*.h"
      - "**/*.S"
      - "**/*.inc"
      - "**/*.py"
      - "**/meson.build"
      - "meson_options.txt"
      - "meson.options"
      - "latxbuild/**"
      - "configs/**"
      - "**/*.mak"
      - ".gitmodules"
      - "configure"
      - "Makefile"
      - "GNUmakefile"
      - "meson"
      - "meson/**"
      - "runtime/**"
      - "tests/runtime/**"
      - ".github/.ci/**"
      - ".github/workflows/**"
  pull_request:
    paths:
      - "**/*.c"
      - "**/*.h"
      - "**/*.S"
      - "**/*.inc"
      - "**/*.py"
      - "**/meson.build"
      - "meson_options.txt"
      - "meson.options"
      - "latxbuild/**"
      - "configs/**"
      - "**/*.mak"
      - ".gitmodules"
      - "configure"
      - "Makefile"
      - "GNUmakefile"
      - "meson"
      - "meson/**"
      - "runtime/**"
      - "tests/runtime/**"
      - ".github/.ci/**"
      - ".github/workflows/**"

permissions:
  contents: read

# Test registration and suite selection rules are documented in tests/README.md.
concurrency:
  # Keep reusable calls separate from the caller and from Clang.
  group: lat-tests-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref == 'refs/heads/master') }}

jobs:
  select-matrix:
    name: Select CI coverage
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      build_types: ${{ steps.matrix.outputs.build_types }}
      test_containers: ${{ steps.matrix.outputs.test_containers }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Select PR or full matrix
        id: matrix
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python3 scripts/ci/lat_ci_matrix.py

  meson-fast:
    needs: select-matrix
    name: "test (${{ matrix.container.name }}, lat-pr-fast)"
    runs-on: ubuntu-latest
    timeout-minutes: 40

    strategy:
      fail-fast: false
      matrix:
        container: ${{ fromJSON(needs.select-matrix.outputs.test_containers) }}

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Cache test compilation
        uses: actions/cache@v6
        with:
          path: /tmp/lat-test-cache
          key: tests-v2-${{ matrix.container.name }}-${{ matrix.container.sanitizers }}-${{ hashFiles(matrix.container.dockerfile) }}-${{ github.sha }}
          restore-keys: |
            tests-v2-${{ matrix.container.name }}-${{ matrix.container.sanitizers }}-${{ hashFiles(matrix.container.dockerfile) }}-
            tests-v1-${{ matrix.container.name }}-${{ matrix.container.sanitizers }}-

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v4

      - name: Run fast Meson tests
        run: |
          docker run --rm \
            --platform linux/loong64 \
            --env CCACHE_DIR=/root/.cache/ccache \
            --volume /tmp/lat-test-cache:/root/.cache \
            --volume "$(pwd):/io" \
            --workdir /io \
            "ghcr.io/${{ github.repository_owner }}/${{ matrix.container.name }}:${{ matrix.container.tag }}" \
            sh -eux -c '
              ${{ matrix.container.prepare_meson }}
              meson --version
              # Older Fedora images did not include ccache; keep the first
              # full run working while the updated image is being published.
              command -v ccache >/dev/null || dnf install -y ccache
              ccache --zero-stats
              trap "ccache --show-stats" EXIT
              export CFLAGS="-Wno-error=unused-but-set-variable -Wno-error=unused-function -Wformat -Werror=format-y2k"
              mkdir -p build64-tests
              cd build64-tests
              sanitizer_flags=
              if [ "${{ matrix.container.sanitizers }}" = true ]; then
                sanitizer_flags="--enable-debug --enable-sanitizers"
              fi
              ../configure \
                --target-list=x86_64-linux-user,i386-linux-user \
                --enable-latx \
                --enable-tests \
                --disable-werror \
                --optimize-O1 \
                --extra-ldflags=-ldl \
                --disable-docs \
                --meson=meson \
                --with-git-submodules=ignore \
                $sanitizer_flags
              cd ..
              export LATX_RUNTIME_TEST_SKIP_GUEST_EXECUTION=1
              ASAN_OPTIONS=detect_leaks=0 \
              UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
                meson test \
                  -C build64-tests \
                  --suite lat-pr-fast \
                  --print-errorlogs
            '
